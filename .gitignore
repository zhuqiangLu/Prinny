__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.DS_Store
build/
dist/

# Cached paper PDFs — the store lives outside the repo (~/.prinny/pdfs). These are
# named <paper-id>.pdf and only ever land here if pdf_store_path resolves to the CWD
# (now guarded against in app/pdf_store.store_dir). Never commit them — they're a cache.
*.pdf

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.playwright-mcp/

# External components (their own histories; not part of this repo)
llm_wiki/
zotero-arxiv-daily-local/

# Vendored Tailwind standalone binary (76MB; fetched by scripts/build_css.sh)
bin/tailwindcss
